一.管理修改

    git checkout -- [name]    

    git reset HEAD [name]  git checkout [name]

    git reset --hard HEAD [comid]

二.关联远程库

    git remote add ori_test1 [url]

    git push -u ori_test1 master

    git push ori_test1 master

    git clone [url]

三.分支管理

  git checkout -b [name]

  git checkout [name]

  git branch -d [name]

  git merge [name]
  
  git pull 
  
  git fetch

  git branch

  git stash

  git stash list

  git stash apply

  git stash drop

  git stash pop

四.多人协作

  git fetch [远程库名] [本地库名]:[二者的别名]   git diff [比较二者的别名]  git merge [二者的别名] 

  git pull

